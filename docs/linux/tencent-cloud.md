# Tencent Cloud

- 轻量应用服务器

```shell
node -v
nginx -v
# /usr/local/lighthouse/softwares/nodejs
# /usr/local/lighthouse/softwares/nginx

# Nginx 1.20.2 主配置文件
# /usr/local/lighthouse/softwares/nginx/conf/nginx.conf

curl localhost:80
curl 0.0.0.0:80

# master 进程
# worker 进程
ps -ef | grep nginx

sudo lsof -i:80
sudo lsof -i:443
```
